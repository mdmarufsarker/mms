#include <bits/stdc++.h>
using namespace std;

int main(){
    int a, b;
    cin >> a >> b;

    // if(a >= b) cout << "Yes" << endl;
    // else cout << "No" << endl;

    // (a >= b) ? cout << "Yes" << endl : cout << "No" << endl;

    cout << (a >= b ? "Yes" : "No") << endl;
}
